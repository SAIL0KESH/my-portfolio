# Needed for GitHubStats (local dev):
NEXT_PUBLIC_BASE_URL=http://localhost:3000
GITHUB_USERNAME=https://github.com/SAIL0KESH

# Contact form SMTP (optional until ready)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=sanisettysailokesh@gmail.com

# S3 presign for resume (optional; if omitted we serve /public/resume.pdf)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=<your-bucket>
S3_KEY=resume/sai-lokesh.pdf

# DynamoDB table for visitors (optional; enables real counter)
DDB_TABLE=PortfolioVisitors
DDB_PARTITION_KEY=id
DDB_PARTITION_VALUE=global

